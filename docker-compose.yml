services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - ${DATABASE_PORT:-5432}:5432
    environment:
      POSTGRESQL_USERNAME: ${DATABASE_USERNAME:-todo}
      POSTGRESQL_PASSWORD: ${DATABASE_PASSWORD:-todo}
      POSTGRESQL_DATABASE: ${DATABASE_NAME:-todo}
    volumes:
      - "postgresql_data:/bitnami/postgresql"

volumes:
  postgresql_data:
    driver: local
